/**
 * Production entrypoint is backend/server.js.
 *
 * This root server.js is preserved only for legacy compatibility when the
 * repository is started from the repository root. Deployments must use
 * `node backend/server.js`.
 */

require('./backend/server');
