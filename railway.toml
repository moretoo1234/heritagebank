[build]
builder = "nixpacks"

[build.nixpacks]
node = "20"
buildCommands = ["npm ci --prefix backend || npm install --prefix backend"]

[deploy]
startCommand = "node backend/server.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
