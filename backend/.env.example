# Local development environment variables for the backend API (example)
# Copy this file to `backend/.env` and fill in your real values.
# NOTE: `backend/.env` is ignored by git.

# Server
PORT=3001
NODE_ENV=development

# Database (MySQL/TiDB)
DB_HOST=localhost
DB_PORT=4000
DB_USER=
DB_PASSWORD=
DB_NAME=heritage_bank

# Auth
JWT_SECRET=dev-jwt-secret-change-me

# Optional: auto-create an admin on first boot (only if ADMIN_PASSWORD is set)
ADMIN_EMAIL=admin@heritagebank.com
ADMIN_PASSWORD=

# Branding / banking constants
ROUTING_NUMBER=091238946
# Database Configuration
DB_HOST=your-database-host.com
DB_PORT=4000
DB_USER=your-db-username
DB_PASSWORD=your-db-password
DB_NAME=your-database-name

# Security
JWT_SECRET=your-super-secret-jwt-key-change-this

# Admin Account (created on first run)
ADMIN_EMAIL=admin@heritagebank.com
ADMIN_PASSWORD=YourSecureAdminPassword

# Server
PORT=3001
ROUTING_NUMBER=091238946
